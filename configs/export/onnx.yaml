checkpoint_path: ${infer.checkpoint_path}
vocab_path: ${infer.vocab_path}
output_path: models/onnx/transformer_htr_forward.onnx

opset_version: 17
batch_size: 1
target_sequence_length: 64
validate: true
dynamic_axes: true
